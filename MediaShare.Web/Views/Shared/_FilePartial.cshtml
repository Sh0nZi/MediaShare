@using MediaShare.Web.Models.Files;

@model BasicMediaFileViewModel

@Html.DisplayFor(f => f.Title, "DisplayMediaTitle", new { id = @Model.Id })
@{
    Html.RenderAction("Index", "Thumbnail", new { id = @Model.Id, area = "" });
}


